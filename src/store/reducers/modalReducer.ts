const initState = {};

export const modalReducer = (state = initState, action:any) => {
  switch (action.type) {
    case '':
      return
      
  
    default:
      return state
  }
};
